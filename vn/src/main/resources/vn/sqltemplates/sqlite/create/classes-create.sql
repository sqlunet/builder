CREATE TABLE IF NOT EXISTS ${classes.table} ( ${classes.classid} INTEGER NOT NULL, ${classes.class} VARCHAR (64) NOT NULL, ${classes.classtag} VARCHAR (32) NOT NULL,PRIMARY KEY (${classes.classid}));
