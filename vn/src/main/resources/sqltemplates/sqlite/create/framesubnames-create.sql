CREATE TABLE IF NOT EXISTS ${framesubnames.table} (
    ${framesubnames.framesubnameid} INTEGER NOT NULL,
    ${framesubnames.framesubname} VARCHAR (64) NOT NULL,
PRIMARY KEY (${framesubnames.framesubnameid}));
