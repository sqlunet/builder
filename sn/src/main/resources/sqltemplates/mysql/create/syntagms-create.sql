CREATE TABLE ${syntagms.table} (
    ${syntagms.syntagmid} INTEGER NOT NULL,
    ${syntagms.synset1id} INTEGER NOT NULL,
    ${syntagms.word1id} INTEGER NOT NULL,
    ${syntagms.synset2id} INTEGER NOT NULL,
    ${syntagms.word2id} INTEGER NOT NULL,
PRIMARY KEY (${syntagms.syntagmid}));
