CREATE INDEX IF NOT EXISTS k_${members.table}_${members.rolesetid} ON ${members.table} (${members.rolesetid});
CREATE INDEX IF NOT EXISTS k_${members.table}_${members.pbwordid} ON ${members.table} (${members.pbwordid});
