CREATE TABLE IF NOT EXISTS ${governors_annosets.table} (
${governors_annosets.governorid} INTEGER NOT NULL,
${governors_annosets.annosetid} INTEGER NOT NULL,
PRIMARY KEY (${governors_annosets.governorid},${governors_annosets.annosetid}) );
