CREATE INDEX IF NOT EXISTS k_${annosets.table}_${annosets.frameid} ON ${annosets.table} (${annosets.frameid});
CREATE INDEX IF NOT EXISTS k_${annosets.table}_${annosets.luid} ON ${annosets.table} (${annosets.luid});
